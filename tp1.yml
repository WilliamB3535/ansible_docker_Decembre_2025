---
- name: Install and configure apache 2
  hosts : all
  remote_user: root

  tasks:
    - name: install apache2
      apt:
        name: apache2
        state: present
        update_cache: yes

    - name: ensure apache2 is running
      service:
        name: apache2
        state: started
        enabled: yes
